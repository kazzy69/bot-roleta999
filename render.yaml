services:
  - type: web
    name: bot-roleta999
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: PYTHONUNBUFFERED
        value: "TRUE"

